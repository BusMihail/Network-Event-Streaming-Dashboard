services:
  mongodb:
    image: mongo:7.0
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
    # TODO: add volumes, networks, and other configuration

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    depends_on:
      - zookeeper
    # TODO: add volumes, networks, and other configuration

  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.0
    container_name: zookeeper
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    # TODO: add volumes, networks, and other configuration

  # TODO: add node-service configuration
  # node-service:
  #   build: ./node-service
  #   container_name: node-service
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - mongodb
  #     - kafka

  # TODO: add java-service configuration
  # java-service:
  #   build: ./java-service
  #   container_name: java-service
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - mongodb
  #     - kafka

  # TODO: add frontend configuration
  # frontend:
  #   build: ./frontend
  #   container_name: frontend
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - node-service
  #     - java-service
